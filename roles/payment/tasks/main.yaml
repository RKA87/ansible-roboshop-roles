- name: "Application setup for {{ component }}"
  ansible.builtin.import_role:
    name: common
    tasks_from: app-setup.yaml

- name: "Install Python and its dependencies for {{ component }}"
  ansible.builtin.import_role:
    name: common
    tasks_from: python-setup.yaml

- name: "Systemd Setup for {{ component }}"
  ansible.builtin.import_role:
    name: common
    tasks_from: systemd-setup.yaml

- name: "Start and Enable {{ component }} service"
  ansible.builtin.systemd.service:
    name: "{{ component }}"
    state: restarted
    enabled: yes